# 2026-09-15 Supabase 자동 유지 점검 배포 전 보고서

## 판정

- 결과: `NO-GO`
- 단계: `PRE-DEPLOY`
- 작성 일시: 2026-09-15 11:13 KST
- 검증자: Codex

## 검증 대상

- 프로젝트: bookstore-news-studio
- 환경: 격리된 로컬 기능 브랜치, Production 읽기 전용 상태 확인
- 기준 브랜치: `main` (`3affe71`)
- 대상 브랜치: `codex/m5-supabase-keepalive`
- 커밋 SHA: `611b14a`
- 배포 버전: GitHub Actions workflow, 아직 기본 브랜치에 미병합
- 배포 URL: `https://bookstore-news-studio.rlawksml.chatgpt.site`

## 변경 기능과 TC

| 변경 기능 | 영향 TC | 자동화 파일 | 배포 전 | 배포 후 |
|---|---|---|---|---|
| 8시간 주기 Production Supabase 읽기 전용 keepalive | TC-OPS-001 | `apps/web/tests/production-keepalive-workflow.test.mjs` | 필수 | 최초 수동 실행 필수 |

## 결과 요약

| PASS | FAIL | BLOCKED | SKIPPED | 전체 |
|---:|---:|---:|---:|---:|
| 6 | 1 | 0 | 2 | 9 |

## TC 실행 결과

| TC ID | 우선순위 | 종류 | 결과 | 실제 결과·증거 | 비고 |
|---|---|---|---|---|---|
| TC-OPS-001 | P1 | 자동 정적 검사 | PASS | 예약 주기, 공개 GET, 본문 폐기, 재시도, 쓰기·비밀키 부재 1/1 통과 | workflow 활성화 전 |
| LINT | P0 | 정적 분석 | PASS | `npm run lint` 오류 없음 |  |
| BUILD | P0 | production build | PASS | vinext build 완료 | 기존 chunk 크기 경고만 존재 |
| REGRESSION | P0 | 자동 회귀 | FAIL | 25/26 통과, 공개 달력 테스트의 `2026년 7월` 고정값 1건 실패 | 기존 PR #41에서 수정·검증 완료, 미병합 |
| PROD-HOME | P0 | 운영 읽기 스모크 | PASS | `/` HTTP 200 | 운영 데이터 변경 없음 |
| PROD-WORKSPACE | P0 | 운영 읽기 스모크 | PASS | `/api/workspace` HTTP 200 | 응답 본문 미기록 |
| PROD-DATA-BASELINE | P0 | Supabase 읽기 전용 | PASS | DB 8/12/0/2행, 소식 46건, 원본·미리보기 49/49개 | 2026-09-07 기준선과 일치 |
| SUPABASE-INTEGRATION | P0 | 실제 DB·Storage 쓰기 | SKIPPED | 운영 데이터 보호를 위해 실행하지 않음 | 이번 workflow는 쓰기 기능 없음 |
| PLAYWRIGHT | P1 | 브라우저 | SKIPPED | 사용자 UI·앱 런타임 변경 없음 | workflow 수동 실행으로 대체 예정 |

## 실패·차단 상세

### REGRESSION — 공개 달력 현재 월 고정값

- 상태: FAIL
- 영향: 현재 날짜가 2026년 7월이 아니면 `npm test`가 실패해 PR 필수 검증을 통과할 수 없다.
- 재현 단계: `cd apps/web && npm test`
- 기대 결과: 한국 시간의 현재 월이 렌더링되고 전체 테스트가 통과한다.
- 실제 결과: 화면은 `2026년 9월`을 올바르게 렌더링하지만 기존 테스트가 `/2026년 7월/`을 기대해 1건 실패한다.
- 로그·증거: GitHub PR #41은 고정 월을 `Asia/Seoul` 현재 월로 교체했고 기존 CI 25/25를 통과했다.
- 우회 방법: 테스트를 생략하거나 기대값을 느슨하게 하지 않는다.
- 후속 작업: 사용자의 `main` 병합 승인을 받은 뒤 PR #41을 먼저 병합하고 기능 브랜치를 최신화해 전체 회귀를 다시 실행한다.

## 실행 명령과 환경

| 구분 | 명령·환경 | 결과 |
|---|---|---|
| lint/type | `npm run lint` | PASS |
| build | `npm test` 안의 `npm run build` | PASS |
| unit | `node --test tests/production-keepalive-workflow.test.mjs` | PASS 1/1 |
| regression | `npm test` | FAIL 25/26, 기존 시간 의존 테스트 |
| integration | Production Supabase SELECT·Storage 목록 조회 | PASS, 읽기 전용 |
| smoke | Production `/`, `/api/workspace` GET | PASS, HTTP 200 |

## 테스트 데이터 정리

- DB 테스트 행: 생성하지 않음
- Storage 원본: 생성·수정·삭제하지 않음
- Storage 미리보기: 생성·수정·삭제하지 않음
- 편집 잠금·presence: 생성하지 않음
- 정리 확인: 정리 대상 없음. 운영 데이터 변경 없음.

## 경고와 잔여 위험

- GitHub 예약 workflow는 기본 브랜치에 병합된 뒤에만 실행된다.
- GitHub는 장기간 저장소 활동이 없으면 예약 workflow를 중지할 수 있다.
- 읽기 요청은 Free Plan 자동 일시정지 가능성을 낮추지만 Supabase 자체 장애와 무중단을 보장하지 않는다.
- `main`의 기존 의존성 audit 경고는 v1.1 보안 PR #67 범위이며 이번 운영 PR에 섞지 않는다.

## 최종 결론

- 판정 근거: 새 TC·lint·build·운영 읽기 검증은 통과했지만 필수 전체 회귀 1건이 실패했다.
- 배포 가능 여부: `NO-GO`. PR #41 병합과 전체 26/26 재검증 전에는 keepalive workflow를 기본 브랜치에 병합하지 않는다.
- 배포 후 재확인 항목: workflow 수동 실행 HTTP 200, 로그의 응답 본문·비밀값 부재, Production 데이터 기준선 유지.
