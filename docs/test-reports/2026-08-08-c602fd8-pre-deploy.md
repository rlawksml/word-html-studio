# 2026-08-08 현재 월 테스트 수정 배포 전 보고서

## 판정

- **GO — 테스트 수정 PR 병합 가능**
- 운영 Supabase 프로젝트를 재개한 뒤 공개 홈과 핵심 읽기 API가 모두 정상 응답한다.
- 이번 변경은 자동 테스트의 월 기대값만 수정하므로 애플리케이션 재배포는 필요하지 않다.
- 운영 저장·사진 업로드처럼 상태를 바꾸는 기능은 기존 데이터를 보호하기 위해 이번 점검에서 실행하지 않았다.

## 검증 대상

- 기능 코드 커밋: `c602fd8`
- 변경 파일: `apps/web/tests/rendered-html.test.mjs`
- 변경 목적: `2026년 7월` 고정 기대값을 한국 시간의 실제 현재 월 검증으로 교체

## 실행 결과

| 항목 | 상태 | 결과 |
|---|---|---|
| lint | PASS | 오류 없음 |
| production build | PASS | 빌드 완료, 기존 HEIC 지연 청크 크기 경고만 존재 |
| 자동 회귀 | PASS | 25/25 통과 |
| 운영 홈·도움말·검색 파일 | PASS | 공개 GET 경로 HTTP 200 |
| TC-LOAD-001 공용 데이터 | PASS | `/api/workspace` HTTP 200, 방문자 화면 데이터 표시 확인 |
| 개선사항 조회 | PASS | `/api/improvements` HTTP 200, 등록 내역 2건 표시 확인 |
| Worker 복구 후 로그 | PASS | 복구 뒤 `/`, `/api/workspace`, `/api/improvements` 요청 HTTP 200 |
| Supabase 통합 | SKIPPED | 운영 데이터 보호를 위해 실행하지 않음 |
| 업로드·저장·삭제 | SKIPPED | 별도 테스트 환경 미확인 |

## 운영 복구 확인

- 장애 원인은 `bookstore-news-studio` Supabase 프로젝트가 일시 중지된 상태였던 것이다.
- 대시보드의 프로젝트 재개 기능만 실행했으며 데이터 삭제·초기화·마이그레이션은 수행하지 않았다.
- 재개 후 Supabase REST와 Sites API가 순서대로 정상화됐다.
- 읽기 전용으로 확인한 보존 데이터는 책방 7곳, 2026년 7월 소식 묶음 4건, 개별 소식 5건, 개선사항 2건이다.
- 방문자 화면을 2026년 7월로 이동했을 때 공개 대상 책방 2곳과 소식 2건이 달력과 카드에 표시됐다.
- 복구 직전 Worker에는 Supabase 연결 실패로 인한 HTTP 500이 기록됐으나, 복구 후 같은 API 요청은 HTTP 200이다.

## 운영 데이터 안전

- 운영 환경에는 공개 `GET` 요청과 Worker 로그 조회만 수행했다.
- 책방·소식·사진·개선사항을 생성, 수정, 삭제, 초기화하거나 리셋하지 않았다.
- 로그인 제출, 잘못된 권한 요청, Supabase 통합 테스트, cleanup 명령을 실행하지 않았다.
- **운영 데이터 변경 없음**.

## 남은 위험과 후속 작업

- Supabase 무료 프로젝트는 사용량이 적으면 다시 일시 중지될 수 있다. 상시 공개 서비스라면 유료 플랜 전환 또는 상태 감시·장애 알림을 검토한다.
- 운영 저장·JPEG/HEIC 업로드·삭제 복구는 쓰기 테스트이므로 별도 테스트 프로젝트나 사용자가 승인한 테스트 레코드에서 다시 검증해야 한다.
- PR의 `supabase-integration` 작업은 수동 실행 전용이라 일반 PR CI에서는 `SKIPPED`가 정상이다.
- **운영 데이터 변경 없음**.
